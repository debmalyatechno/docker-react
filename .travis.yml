sudo: required
services:
        - docker
before_install:
        - docker build -t debmalyatechno/docker-react -f Dockerfile.dev .
script:
        - docker run -e CI=true debmalyatechno/docker-react npm run test
